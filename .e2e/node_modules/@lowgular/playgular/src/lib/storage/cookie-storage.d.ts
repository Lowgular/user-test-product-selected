import { BrowserContext } from 'playwright/test';
import { StringOrRegExp } from '../data';
import { Storage } from './storage';
export declare class CookieStorage implements Storage {
    protected browserContext: BrowserContext;
    protected readonly domain: string;
    protected readonly path: string;
    constructor(browserContext: BrowserContext, domain: string, path?: string);
    expectItemExists(key: string): Promise<void>;
    expectItemsExist(keys: string[]): Promise<void>;
    expectItems(items: Record<string, StringOrRegExp>): Promise<void>;
    stubItem(key: string, value: string): Promise<void>;
    stubItems(items: Record<string, string>): Promise<void>;
}
