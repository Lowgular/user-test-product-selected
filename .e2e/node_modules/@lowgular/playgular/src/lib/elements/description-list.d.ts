import { Locator } from '../playwright';
import { SimpleElement, TextElement } from './atoms';
export declare class DescriptionTerm extends TextElement {
    protected readonly listLocator: Locator;
    protected readonly index: number;
    constructor(locator: Locator, listLocator: Locator, index: number);
    details(): Promise<TextElement[]>;
}
export declare class DescriptionList extends SimpleElement {
    constructor(locator: Locator);
    terms(): Promise<DescriptionTerm[]>;
    details(): Promise<SimpleElement[]>;
}
