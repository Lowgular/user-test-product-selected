import { Page } from '@playwright/test';
import { StringOrRegExp } from '../data';
export declare class Router {
    protected readonly page: Page;
    protected readonly appBaseUrl?: string;
    constructor(page: Page, appBaseUrl?: string);
    refresh(): Promise<void>;
    waitForUrl(url: StringOrRegExp): Promise<void>;
    navigate(url: string): Promise<void>;
    navigateFullUrl(fullUrl: string): Promise<void>;
    navigateAndWait(url: string): Promise<void>;
    navigateAndWaitFullUrl(fullUrl: string): Promise<void>;
    expectUrl(url: StringOrRegExp): Promise<void>;
    expectFullUrl(fullUrl: StringOrRegExp): Promise<void>;
    expectAndWaitForUrl(url: StringOrRegExp): Promise<void>;
    expectAndWaitForFullUrl(fullUrl: StringOrRegExp): Promise<void>;
    expectGuardRedirect(fromRoute: string, toRoute: string): Promise<void>;
}
