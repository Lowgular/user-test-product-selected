"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.objectsShouldIntercept = void 0;
const matchers_1 = require("../matchers");
const objectsShouldIntercept = (expected, actual) => {
    const expectedKeys = Object.keys(expected);
    for (let i = 0; i < expectedKeys.length; i++) {
        const expectedKey = expectedKeys[i];
        const expectedVal = expected[expectedKey];
        const actualVal = actual[expectedKey.toLowerCase()];
        if (typeof expectedVal === 'object') {
            const result = (0, exports.objectsShouldIntercept)(expectedVal, actualVal);
            if (result === false) {
                return false;
            }
        }
        else {
            const expectedValRegexp = (0, matchers_1.coerceRegexp)(expectedVal);
            if (`${actualVal}`.match(expectedValRegexp) === null) {
                return false;
            }
        }
    }
    return true;
};
exports.objectsShouldIntercept = objectsShouldIntercept;
//# sourceMappingURL=object.js.map