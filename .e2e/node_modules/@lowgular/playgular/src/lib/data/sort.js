"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.sortData = void 0;
const tslib_1 = require("tslib");
const sortData = (elements, criterion, direction) => tslib_1.__awaiter(void 0, void 0, void 0, function* () {
    const values = [];
    for (let i = 0; i < elements.length; i++) {
        values.push({ index: i, val: yield criterion(elements[i]) });
    }
    const sorted = values.sort((a, b) => direction === 'asc' ? a.val - b.val : b.val - a.val);
    return sorted.map((v) => elements[v.index]);
});
exports.sortData = sortData;
//# sourceMappingURL=sort.js.map