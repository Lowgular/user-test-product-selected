{
  "name": "03-product-selected",
  "scripts": {
    "test": "playwright test",
    "build": "tsc",
    "test:build": "npm run build && playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.52.0",
    "@lowgular/playgular": "latest",
    "typescript": "^5.0.0"
  }
}